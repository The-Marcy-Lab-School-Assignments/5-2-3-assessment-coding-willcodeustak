const bankAccountFactory = (checkBalance,add,subtract) => {

  const obj = {
checkBalance() {
  /** FEEDBACK: What specifically should we be checking to see if there is a value there? */
  if( null ){
    /** FEEDBACK: If the balance is null should we be changing that value to be a number? */
    Number()
  }
return checkBalance
},

add(){
  console.log()
},

subtract(){
},
  }

  return obj

};

module.exports = {
  bankAccountFactory,
};
//test